var name = "Shawn Lee"
var country = "China"
var children = 0

println(name, "lives in $country and has $children")

fn get_message(name str, country str, children int) {
  return "Hey"
}

var message = get_message(name, country, children)

println(message)
