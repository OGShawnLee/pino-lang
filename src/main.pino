var name = "Shawn Lee"
var country = "China"
var is_married = false

println("$name was born in $country", is_married)
println("This was written in Pino and transpiled to both JavaScript and Python!")

name = readln("What is your name? ")
country = readln("What country were you born in? ")

println("$name was born in $country")

val arr_int = []int { len: 6, init: it + 3 }

println(arr_int)