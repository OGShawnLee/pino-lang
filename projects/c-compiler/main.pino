from Lexer import Result, lex

fn main {
	println("Welcome to the Pino C Compiler!")

	val program = "int main(void) { return 2; }"

	match lex(program) {
		when Result::Success(token_list) {
			token_list:each(println)
		}
		when Result::Error(message) {
			println(message)
		}
	}
}