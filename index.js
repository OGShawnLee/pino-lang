let name = "Shawn Lee";
let country = "China";
let children = 0;
console.log(name, `lives in ${country} and has ${children}`);
function get_message(name, country, children) {
return;
}
let message = get_message(name, country, children);
console.log(message);
